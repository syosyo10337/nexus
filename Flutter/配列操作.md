---
tags:
  - flutter
  - dart
created: 2026-01-03
status: active
---

# 配列操作

# 配列へのアクセス

基本は、他の言語と一緒

```Dart
const arry = [0,1,2]
arry[2] => error!
```

## 配列に要素を追加する(addメソッド)

re-assignするのではなく、numbersの内部データ構造を編集するため、finalで定義した配列の変数でも使用できるよ。

## whereメソッド

filterを行う。toList()を後付けすることでiterableオブジェクトから、配列にListに再度変更できる。  

## contains

```Dart
someList.contains(element);
```

## jsonDecode

JSon文字列をdartで扱うJSONオブジェクトに変換する.

## expand

rubyで言うflat_map