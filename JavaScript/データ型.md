 

# データ型

---

目次

---

[データ型の種類](#c929aed0-1b62-425d-bc87-3dab7d78794b)

[String](#08864f9d-23a2-4070-aec0-f5d206655258)

[明示的に型を変換する](#739fef38-be23-4e0b-a331-2eda24e16812)

[テンプレートリテラル(template literals)](#56aff167-d1f2-435f-bd86-c5daf0df1ec0)

[文字列操作のメソッド](#9e8dfe9d-a7f7-48d6-9f3a-e9cfcf9e2c2b)

[部分文字列抽出](#3f864e91-f9ee-47e3-b5d4-a1706d7c8377)

[nullとundefined](#12938cdd-027d-8043-a60e-e61e27f68fb2)

[symbol型](#15938cdd-027d-80c3-a56c-c1efdd19fb77)

# データ型の種類

---

JavaScriptのデータ型は以下のものがあります。　

- String(文字列型)  
    一連のテキストで、文字列と呼ばれます。値が文字列であることを示すには、単一引用符で囲む必要があります。

- Number(数値型)  
    数値です。数字には引用符がありません。

- Boolean(真偽値)  
    論理値です。これは真か偽かの値です。 true と false は特別なキーワードで、引用符は必要ありません。

```JavaScript
let test = 6 < 3;
```

- Array(配列)  
    単一の参照で複数の値を格納できる構造です。

```JavaScript
let myVariable = [1,'Bob','Steve',10]; 

//配列の各メンバーは次のように参照します。
myVariable[0], myVariable[1], など。
```

- Object(オブジェクト)  
    JavaScript のすべてがオブジェクトであり、変数に格納することができます。  
    オブジェクトとは現実世界にあるもの、英語の語彙としてのObject(visible entity)をモデルにしたコードの構造のこと。

```JavaScript
e.g.）
//オブジェクト
let dog = {
	name : "ぽち", breed : "ダルメシアン"
}
//オブジェクトの参照
dog.name;
> "ぽち"
上記のすべての例も同様です。

//JavaScript のオブジェクトデータ型は、関数とデータを保持できる名前空間です
```

# String

---

Javascriptではstrとcharを区別しない。

- ' 'もしくは" "で囲まれたものを文字列リテラル(string literalと呼ぶ。)

- 文字列中で、'',""に干渉する場合は、特殊文字(\n-改行,\t-tab,\文字-文字列として扱う)を使ってエスケープ処理を行う。  
    ex) `console.log('it\'s me');`  
    もしくは、'',""を使い分ける。

- 文字のエスケープとは、
    
    その文字が、コードではなく、文字列の一部であるとブラウザーに認識させる書き方です。
    

- 文字列の連結 --「+」記号を用いて、文字列同士を連結できる。

- ==Javascriptにおいて、'文字列' + 数値 を +オペレータで操作する時に、暗黙の型変換によって、文字列に変換される。==

### 明示的に型を変換する

- `Number()`関数  
    渡された文字列？をすべてnum型に変換する関数。

- `toString()`メソッド  
    全てのnumが持つメソッドの一つで、文字列に変換する。

## テンプレートリテラル(template literals)

文字列構文の一つで、  
バッククォート（`）によって囲まれた文字列の中で,プレースホルダーと呼ばれる${定数または変数}を記すと、文字列の中に定数や変数を埋め込むことができる。  
また、ソースコード中の改行をそのまま、再現できる。

## 文字列操作のメソッド

```JavaScript
let browserType = 'mozilla';

//長さを調べる
browserType.length;

//配列のように[]indexで文字列から文字を取り出す。
//0-based
browerType[2];

//文字列の最後の文字を取り出したい時
browserType[browserType.length-1];

```

### 部分文字列抽出

- `indexOf`  
    もし対象の文字列中に部分文字列が見つかった場合、このメソッドは部分文字列のインデックス位置を表す==数値== (対象の文字列上で部分文字列が始まる文字数) を==返します。==もし対象の文字列中に部分文字列が見つからなかった場合は、==-1 の値を返します。==

```JavaScript
browserType.indexOf('zilla');
```

- 特定の文字列が含まれているかについて調べるより効果的な方法は、  
    indexOfが一致するものがない場合に-1を返すことを利用した条件分岐をさせること。

```JavaScript
if(browserType.indexOf('mozilla') === -1) {
  // もし部分文字列 'mozilla' が含まれていない場合の処理
}
もしくは、
if(browserType.indexOf('mozilla') !== -1) {
  // もし部分文字列 'mozilla' が含まれている場合の処理。
}
```

- `slice(indexStart[, indexEnd])   /substring(indexStart[, [indexEnd])`  
    文字列の開始位置と終了位置がわかっている場合に、そのindexを指定して、抽出する。indexEndを指定しないと、開始位置から文字列の最後までを抽出する。  
    切り出された部分の文字列を返す。

```JavaScript
browserType.slice(0,3);
//>'moz'
//なぜならば0-m 1-o 2-z 3-i...というようにコンピュータ上では対応付けされているため、(0,3)で指定すると0--3の間にある文字列を抽出できる。
```

- `toLowerCase()/toUpperCase()`  
    引数として渡された文字列のすべての文字の大文字・小文字を切り替えます

- 文字列の置換 replace(substr, newSubstr)  
    正規表現やreplceFunctionを引数にとって高度な操作をすることもできる。  
    変数の破壊的変更は行わないので、変数自体の値を変更させたい時は

browserType = browserType.replace('moz','van');

```Plain
cf)
<https://developer.mozilla.org/ja/docs/Web/JavaScript/Reference/Global_Objects/String/replace>
```

# nullとundefined

プリミティブ型ですね、ただ、

string やnumberは　JavaScriptには、プリミティブ型をオブジェクトに自動変換する機能があります。この機能はオートボクシング(autoboxing)によってオブジェクトとして扱われている。

# symbol型