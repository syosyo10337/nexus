---
tags:
  - javascript
  - dom
created: 2026-01-03
status: draft
---

# Documentオブジェクト

querySelctor(””）

CSSセレクターを問い合わせるメソッド

—documentの要素を参照して、変数に代入できたりする。

この時代入されるオブジェクト(ページに存在する要素)は、全て`style`プロパティを持っていてCSS のインラインスタイルで要素に適用されるすべてのプロパティにアクセスすることができます

e.g.)

`guesses.style.backgroundColor = 'yellow';`

## **location.href**

現在のページのURLを持っているプロパティ(webブラウザ上にてPj)