 

# 変数展開

例えば、`RELEASE_BRANCH` が `release/1.2.3` の場合、`RELEASE_VERSION=${RELEASE_BRANCH#release/}` は `RELEASE_VERSION` に `1.2.3` を代入します。

以下は、パラメータ展開の構文の説明です：

- `${variable#pattern}`: `variable` の値の先頭から最短一致する `pattern` を削除します。

- `${variable##pattern}`: `variable` の値の先頭から最長一致する `pattern` を削除しま