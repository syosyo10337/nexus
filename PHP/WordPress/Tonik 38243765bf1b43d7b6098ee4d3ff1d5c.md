 

# Tonik

# template()関数について

tonikの用意している関数で、パーシャルの読み込みとデータを受け渡しを行うことができる。

```PHP
template('button', ['title' => 'Click me!']);


<button><?= $title ?></button>
この部分には、Clik me!が挿入される。 .tpl.phpである必要があります。
```

  
  
純正のwordpress関数を用いても、`get_template_part()`の第三引数をとることでパラメータを渡すことはできる。