---
tags:
  - web
  - protocol
created: 2026-01-04
status: draft
---

# (draft)HTTPステータスコード

# そもそも

200番台は正常系

400はクライアント側のエラー

500はサーバ側のエラー

# 499

クライアントによって通信の接続を切られた場合のエラー

ELB(ロードバランサー)などによって、サーバ側でタイムアウトが設定されている際にも発生するらしい。

- JSONとして送信するパラメータの最後の要素に(,)をつけるとJSONとしては正しくありません。  
    `400 Bad request`になるでしょう

cf)

[JSON](../JavaScript/JSON%2077216dac8b764f01a07e61888561e9a7.html)