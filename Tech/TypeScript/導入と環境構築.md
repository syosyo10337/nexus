---
tags:
  - typescript
  - class
  - tooling
  - data-structure
created: 2026-01-03
status: active
---

# 導入と環境構築

---

一応TypeScriptの導入にはなっていますが、今回はdockerを使って、reactの実行環境込みで構築しました。

# step1. Nodeを導入する

## Node.jsとは[](https://typescriptbook.jp/tutorials/setup#nodejs%E3%81%A8%E3%81%AF)

Node.jsは、JavaScriptをサーバーサイドで動かすことを目的に開発されたソフトウェアです。Node.jsはGoogle ChromeのJavaScriptエンジンと同じ「V8」でJavaScriptを実行します。

Node.jsにはGoogle Chromeと同じJavaScriptエンジンが乗っていると言っても、組み込みのAPIは異なります。ブラウザはJavaScriptからHTMLを操作する必要があるのでDOM APIがありますが、Node.jsにはありません。代わりにNode.jsには、サーバーサイドのさまざまなリソースを制御する必要があるので、ファイルシステムにアクセスするAPIやHTTPサーバーを建てるためのAPI、プロセスを起動したり終了したりするAPIなどがあります。

### *MacOSの場合

```Bash
$ brew install node@16
```

パスも通しましょう

```Bash
echo 'export PATH="/usr/local/opt/node@16/bin:$PATH"' >> ~/.zshrc
```

### *コンテナ環境の場合

[Dockerでのnode実行環境構築](../Node%20js/Docker%E3%81%A7%E3%81%AEnode%E5%AE%9F%E8%A1%8C%E7%92%B0%E5%A2%83%E6%A7%8B%E7%AF%89%200521c3e0b8af4c7eaac1a475a8367263.html)

入っているか確認して下さい。

```Bash
node -v 
```

# Step2. TypeScriptの導入

```Bash
npm install -g typescript
```

正常にインストールされたかをtscのバージョンで確認します。

```Bash
$ tsc -v
```

# ~~(deprecated) Step3. create-react-appで雛形を作成~~

[Dockerでのnode実行環境構築](../Node%20js/Docker%E3%81%A7%E3%81%AEnode%E5%AE%9F%E8%A1%8C%E7%92%B0%E5%A2%83%E6%A7%8B%E7%AF%89%200521c3e0b8af4c7eaac1a475a8367263.html)

## 後入れTSを導入する場合

### 1. パッケージをインストール

```Shell
bun add -d typescript @types/react @types/react-dom
```

### 2. tsconfig.jsonファイルを作成

たとえばベーシックな設定として以下。

```Shell
{
  "compilerOptions": {
    "target": "ESNext",
    "useDefineForClassFields": true,
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "allowJs": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "Node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": ["src"],
  "references": []
}
```