---
tags:
  - react
  - hooks
  - state
created: 2026-01-03
status: active
---

# 制御構文とフォームの制御で気をつける点

ℹ️

かならずkeyプロパティに値を設定すること

もし無ければ、Firerツリーとの差分比較を行う時に、全ての要素を比較して、すべてのDOMを更新しようとして効率が悪い

keyプロパティをつけると、keyプロパティの値を元にFiber Treeの差分を比較する。

# input/textareaの使い方

htmlでいうfor属性をlabel要素に対してつける際には `htmlFor` を使う理由はforはJS上の予約語であるから

ℹ️

## textareaの注意点

textarea要素、JSXの際には、HTMLとは異なり<textarea>タグで囲まないこと。中身の値はvalueプロパティに含める。

## Radioボタン使い方

```TypeScript
{RADIO_COLLECTION.map(item => {
        return (
          <label key={item}> //keyの設定も忘れずに
            <input  
            type="radio"
            value={item}
            checked={fruit === item} //ここでは、mapで対象になっている配列要素と、useStateで管理されているstateとの値を比較している。
						onChange={onChange} 
			//プロパティの定義を直接書いてもいいけど、mapで回さない時には、重複するので、returnの前に定義するとDRY            />
            {item}
          </label>
        );
})};
```

## checkBox使い方

type checkBoxにしてますけどね。

## マルチセレクトボックス

- HTMLの場合には、checked属性自体が付与されているがどうか。によって判断される。しかしJSXにおいては、それぞれのエレメントのcheckedプロパティの真偽値によって判定される。

```TypeScript

import { useState } from "react";

const Example = () => {
  const [fruits, setFruits] = useState([
    { label: "Apple", value: 100, checked: false },
    { label: "Banana", value: 200, checked: false },
    { label: "Cherry", value: 300, checked: false },
  ]);

  const [sum, setSum] = useState(0);

  const handleChange = (e) => {
    const newFruits = fruits.map(fruit => {
      const newFruit = { ...fruit};
      if( newFruit.label === e.target.value) {
        newFruit.checked = !fruit.checked;
      }
      return newFruit;
    })

      setFruits(newFruits);

      //俺の回答&&を使ったショートサーキット
      let sumVal = 0;
      newFruits.forEach(fruit => {
        fruit.checked && (sumVal += fruit.value);
      });
      //Plainな記法
      // newFruits.forEach(fruit => {
      //   if(fruit.checked) {
      //     sumVal += fruit.value;
      //   }
      // });
      //filterで配列を置換してから、forEachでvalueの値を回す。
      // newFruits
      //   .filter(fruit => fruit.checked)
      //   .forEach(fruit => (sumVal += fruit.value));
      setSum(sumVal);
    };

  return (
    <div>
      {fruits.map(fruit => {
        return (
            <div key={fruit.label}>
              <input 
                id={fruit.label}
                type="checkbox"
                value={fruit.label}
                checked={fruit.checked}
                onChange={handleChange} 
              />
              <label htmlFor={fruit.label}>
                {fruit.label}:{fruit.value}
              </label>
            </div>
        );
      })}
      <div>合計：{sum}</div>
    </div>
  );
};

export default Example;
```

# Pull down

- 選択されている要素は、HTMLの場合には、選択されているoption要素に対して、selected属性自体が付与されているかで評価されるが、JSXの場合には、select要素自体のvalueプロパティに選択したいoption要素のvalue属性を格納する形にしてあげる。

```TypeScript
e.g.)
return (
<>
<select
value={selected}
onChange={(e) => setSelected(e.target.value)}
>
<option value="Apple">Apple</option>
<option value="Banana">Banana</option>
<option value="Cherry">Cherry</option>
</select>
√
<div>選択されたくだもの: {selected}</div>
</>
)
```

# formタグで全体を囲むと、Enterキーでもbuttonのクリックと同様にイベントを発火できる。

ただし、同時にページ遷移もしてしまうため、入力値が保持されない。。。  
<form onSubmit={/_ここ_/}>ここで発火するイベントハンドラーに対して、

const theEvent = (e) => {  
e.preventDeafult();  
};  
//として、デフォルト動作をキャンセルする必要があります。

# useStateを使わずにformを実装する。

cf. [https://qiita.com/nuko-suke/items/1393995fd53ecaeb1cbc](https://qiita.com/nuko-suke/items/1393995fd53ecaeb1cbc)

```TypeScript
import { FormEventHandler } from "react";

const Form = ({ createTodo }: { createTodo: (content: string) => void }) => {
  const handleSubmit: FormEventHandler<HTMLFormElement> = (e: React.FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    const formData = new FormData(e.currentTarget as HTMLFormElement);
    const inputEl = formData.get("todoInput") as string;
    createTodo(inputEl);
    e.currentTarget.reset();
  }

  return (
    <form onSubmit={(handleSubmit)}>
      <input type="text" name="todoInput" />
      <button type="submit">追加</button>
    </form>
  )
}

export default Form;
```