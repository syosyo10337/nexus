---
tags:
  - html-css
created: 2026-01-04
status: draft
---

# セレクタ

# [セレクターの組み合わせ]

- `a, b` --(aORb)aまたはbの持つ要素を指定(セレクタのグループ化)

- `ab` --(aANDb)aであり、かつbの要素を持つものを指定

- `a > b` --aの直下の子要素を指定

- `a b` --aの子要素を指定

- `a+b` --aの直後にあるb指定