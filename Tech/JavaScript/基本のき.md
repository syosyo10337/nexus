---
tags:
  - javascript
  - async
  - dom
created: 2026-01-03
status: active
---

# 基本のき

---

```JavaScript
//厳密なエラーチェックをするための文、書き出しに記述する。
'use strict'; 

//丸括弧 () 内に入力された内容をコンソールに出力
console.log();	

//HTMLに記述する時は<script>タグを記述する。

//bodyに書く場合は、CSSなどが読み込まれている前提が必要なので、
//最後に書く。この方法の問題点はパフォーマンスが低下する可能性がある。
<body>
  <script>  
    'user strict'
    
    console.log("hello world");
  
  </script>
	or
	<script src="<ファイルパス>"></script>
</body>


// //でコメントアウト
/*
複数行コメントアウト　
*/

//defer属性で遅延読み込み？外部スクリプトに対して有効
//<script> 要素に達した後も HTML コンテンツのダウンロードを続行するようブラウザーに指示します。
<script defer></script>
```

## asyncとdefer

---

### async属性

asynchronous(非同期)の意味

`async`属性をつけてスクリプトを読み込むと、ページをブロックすることなくスクリプトをダウンロードします。 しかし、**ダウンロードが完了すると、スクリプトが実行され**、ページのレンダリングがブロックされます。 複数のスクリプトが特定の順序で実行されるという保証はありません。 ページ内のスクリプトが互いに独立して実行され、ページ内の他のスクリプトに依存しない場合は、 `async`を使用するのが最もよい方法です。

依存関係がない場合は、こちらのほうが早く処理できる。

### defer属性

`defer`属性で読み込まれたスクリプトは、ページに表示されている順番に読み込まれます。 **ページのコンテンツがすべて読み込まれるまで実行されません。**これは、スクリプトが DOM に依存している場合（例えば、ページ上の 1 つ以上の要素を変更する場合）に便利です。

[![](Screen_Shot_2022-11-06_at_13.08.09.png)](%E5%9F%BA%E6%9C%AC%E3%81%AE%E3%81%8D/Screen_Shot_2022-11-06_at_13.08.09.png)